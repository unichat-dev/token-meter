# ─────────────────────────────────────────────────────────────
# Secrets & user data — NEVER commit these (this is an OSS repo)
# ─────────────────────────────────────────────────────────────
*.env
.env*
secrets.plist
Secrets.swift
Secrets.xcconfig
*.p12
*.mobileprovision
*.provisionprofile
*.cer
*.certSigningRequest
AuthKey_*.p8
# Local test data / real logs copied for debugging
sample-data/private/
*.jsonl.local
fixtures/real/
# Release signing/notarization secrets (identity, team ID, API keys)
scripts/release.env

# ─────────────────────────────────────────────────────────────
# Local planning / working notes — not part of the public repo
# ─────────────────────────────────────────────────────────────
TASKS.md
RESEARCH.md
notes/
*.local.md

# ─────────────────────────────────────────────────────────────
# Distribution / release material — kept local, NOT in the public repo.
# The app source (and README screenshots) stay tracked; only the shipping
# mechanics — release runbook + notarizing build script — are private.
# ─────────────────────────────────────────────────────────────
docs/RELEASE.md
scripts/build-release.sh
scripts/release.env.example

# ─────────────────────────────────────────────────────────────
# Xcode
# ─────────────────────────────────────────────────────────────
## Generated by XcodeGen from project.yml — never commit
TokenMeter.xcodeproj/
Support/Info.plist

## Coverage / profiling residue
*.profraw

## Build generated
build/
dist/
DerivedData/
*.xcarchive
ExportOptions.plist
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

## Various settings
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout

## Xcode Package / SwiftPM
.swiftpm/
.build/
Packages/
Package.resolved

## Xcode automatic signing / user-specific
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/

## Obj-C / Swift caches
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

## Playgrounds
timeline.xctimeline
playground.xcworkspace

# ─────────────────────────────────────────────────────────────
# macOS
# ─────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.Spotlight-V100
.Trashes

# ─────────────────────────────────────────────────────────────
# Tooling / misc
# ─────────────────────────────────────────────────────────────
*.log
.idea/
.vscode/
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output
