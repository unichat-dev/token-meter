# Screenshots

Drop exported PNGs here to fill in the README's screenshot table. Expected files:

| File | What to capture |
|---|---|
| `menubar.png` | The menu-bar popover dashboard — today's tokens/cost, tiles, 5-hour block row. |
| `history.png` | The desktop window on **History** — Swift Charts tokens/cost over time. |
| `widget.png`  | The WidgetKit widget and/or the floating HUD panel. |

Tips: use a Retina display, capture the window with ⌘⇧4 then Space, and keep
widths roughly consistent so the table renders evenly. Avoid showing any real
project paths or prompts in the frame.
